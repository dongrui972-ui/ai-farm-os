# 路线图

目标：把 Phase A 小功能验证版升级为全模块 Unified Mainline，而不是重写技术栈，也不是移植 PowerShell Demo。

## 已落地（本 PR）

| 层级 | 模块 | 状态 |
| --- | --- | --- |
| P0 | 决策看板 | 完成：今日拍板、KPI、风险与任务 |
| P0 | 数字孪生五层 | 完成：墒情/作物/风险/设备/传感器 |
| P0 | 资产 / 作物 / 水肥 / 任务 | 完成：列表 + 灌溉建议转任务 |
| P1 | 设备 / 智能体 / 诊断 / AI 中心 | 完成：仿真读数、顾问重跑、人工诊断 |
| P2 | 机器人 | **Stub**：无位姿、指令 409 |
| P2 | 车队 / 采后 | 可导航 + 台账 API |
| P3 | 供应商 / 协作 / 架构 | 可导航 + API |
| 辅助 | 工作台 / 茬口历史 | 演示可用 |

## Stub 边界（不要假装已完成）

- 无 REAL 气象站、墒情探针、摄像头流。
- 无泵阀远程闭环；`control_enabled` 恒为 false。
- 机器人无实时 GPS、无遥控、无视频。
- 车队位置不是轨迹。
- AI 不喷药、不启泵、不调度机身。
- 孪生地图是场内示意网格，不是测绘底图。

## 2026-09 主干系统性重构（已完成）

1. **后端分层统一（router / service / schema）**
   - 路由层只保留 HTTP 入参与依赖注入。
   - 新增 `app/services/*` 承载核心业务逻辑，避免路由巨石化。
   - 新增 `app/schemas/*` 承载请求 DTO，写接口参数改为强类型校验。
2. **共享类型与常量**
   - `data_source` 统一为 `REAL | SIMULATION | MANUAL` 的共享类型约束。
   - 孪生层配置、数据源图例、能力边界文案提取到服务常量，减少跨模块硬编码。
3. **前端结构收敛**
   - 新增统一路由注册表 `appRoutes.ts`，导航与路由不再双写。
   - API 请求收敛到共享 `ApiClient`，请求体改为显式输入类型。
   - 增加 `DataPageState` 统一加载/错误壳，减少页面样板代码。
4. **可靠性补强**
   - 新增 `apps/api/smoke_routes.py`，覆盖 P0–P3 主路由与关键写接口。
   - 冒烟脚本显式校验 stub 边界：机器人指令固定 409、灌溉采纳只转任务不控硬件。

## 下一步（接入真实场之后）

1. 为每一类设备增加绑定与心跳，`data_source` 才允许标 `REAL`。
2. 机器人独立安全通道：先遥测，后只读位姿，最后才谈授权运动。
3. 水肥策略与现场 PLC / 阀控解耦，建议层与执行层分权。
4. 诊断接入真实图像管线时，仍强制人工确认。
5. SQLite 可继续用于单场演示；多场再用 `DATABASE_URL` 切到 Postgres。
